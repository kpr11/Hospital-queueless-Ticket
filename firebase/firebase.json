{
  "_comment": "Database rules + functions. From this directory: `firebase deploy --only database` / `--only functions`. Hosting is in the repo-root firebase.json (`firebase deploy --only hosting` from the root, after building frontend/).",
  "database": {
    "rules": "database.rules.json"
  },
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": ["node_modules", ".git", "*.log"],
      "runtime": "nodejs20"
    }
  ]
}
